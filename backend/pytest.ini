[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Pure unit tests (no DB, no network)
    integration: Tests that require a real database or Redis
