# Environment — NEVER commit secrets
.env
.env.local
.env.*.local
# .env.production IS committed on purpose — it only holds the public VITE_ backend URL
# (baked into the client bundle at build time, not a secret). Vercel needs it in the repo.

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
